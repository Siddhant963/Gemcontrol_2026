# ==============================================================================
# GemControl API (Node.js / Express / Mongoose)
# ==============================================================================

# --- Dependencies ------------------------------------------------------------
node_modules/
.npm
.node_repl_history
*.tgz

# --- Environment / secrets -------------------------------------------------------
# .env holds MONGO_URI, JWT_SECRET and Cloudinary keys — never commit it.
.env
.env.*
!.env.example

# --- Runtime data (generated by the running server, not source) ----------------
# Product images and firm assets uploaded through the API.
Uploads/
# Weekly automatic data exports (see Utils/cronJobs.js).
Exports/

# --- Logs ------------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pids/
*.pid
*.seed

# --- Tests / coverage --------------------------------------------------------
coverage/
.nyc_output/

# --- Build output ------------------------------------------------------------
dist/
build/

# --- OS / editor -----------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.vscode/*
!.vscode/extensions.json
.idea/
*.iml
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~
